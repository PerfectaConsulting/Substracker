/* ====== RESET & BASE STYLES ====== */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* ====== CSS VARIABLES (THEME TOKENS) ====== */
:root {
  /* Color Palette */
  --primary: #6366f1;
  --secondary: #8b5cf6;
  --accent: #ec4899;
  --warning: #f59e0b;
  --success: #10b981;
  --info: #3b82f6;
  --dark: #1e293b;
  --light: #f8fafc;

  /* Gradients */
  --gradient-1: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  --gradient-2: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
  --gradient-3: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
  --gradient-4: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
  --gradient-5: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
  --gradient-6: linear-gradient(135deg, #30cfd0 0%, #330867 100%);

  /* Glass Effect */
  --glass: rgba(255, 255, 255, 0.25);
  --glass-border: rgba(255, 255, 255, 0.18);
}

/* ====== BASE ELEMENTS ====== */
body {
  font-family: "Inter", "Segoe UI", Roboto, sans-serif;
  background: linear-gradient(135deg, #1a1c3d 0%, #2d1b69 50%, #0f0c29 100%);
  color: var(--text-1);
  line-height: 1.6;
  min-height: 100vh;
  overflow-x: hidden;
  position: relative;
}

body::before {
  content: "";
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: url("data:image/svg+xml;base64,");
  z-index: -1;
  opacity: 0.4;
}

/* ====== LAYOUT COMPONENTS ====== */
.dashboard-container {
  display: flex;
  height: 100vh;
  overflow: hidden;
}

.sidebar {
  width: 280px;
  background: rgba(30, 41, 59, 0.7);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  border-right: 1px solid var(--glass-border);
  padding: 20px 0;
  display: flex;
  flex-direction: column;
  z-index: 10;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
}

.main-content {
  flex: 1;
  padding: 30px;
  overflow-y: auto;
  position: relative;
}

/* ====== UI COMPONENTS ====== */
/* Logo */
.logo {
  display: flex;
  align-items: center;
  padding: 0 25px 30px;
  border-bottom: 1px solid var(--glass-border);
  margin-bottom: 20px;
}

.logo-icon {
  width: 60px;
  height: 60px;
  background: var(--gradient-1);
  border-radius: 16px;
  margin-right: 15px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-weight: bold;
  font-size: 24px;
  box-shadow: 0 10px 25px rgba(99, 102, 241, 0.4);
  animation: float 3s ease-in-out infinite;
}

.logo span {
  font-size: 24px;
  font-weight: 700;
  background: linear-gradient(90deg, #fff, #c7d2fe);
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
  text-shadow: 0 2px 10px rgba(99, 102, 241, 0.3);
}

/* Navigation */
.nav-menu {
  display: flex;
  flex-direction: column;
  padding: 0 15px;
  flex: 1;
  overflow-y: auto;
}

.nav-link {
  display: flex;
  align-items: center;
  padding: 15px 20px;
  text-decoration: none;
  color: rgba(255, 255, 255, 0.7);
  font-weight: 600;
  font-size: 15px;
  border-radius: 12px;
  margin-bottom: 8px;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.nav-link::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 0;
  height: 100%;
  background: linear-gradient(90deg, var(--primary), var(--secondary));
  transition: width 0.3s ease;
  z-index: -1;
  opacity: 0.7;
}

.nav-link:hover {
  color: #fff;
  transform: translateX(5px);
}

.nav-link:hover::before {
  width: 100%;
}

.nav-link.active {
  background: linear-gradient(90deg, var(--primary), var(--secondary));
  color: #fff;
  box-shadow: 0 5px 15px rgba(99, 102, 241, 0.4);
}

.nav-link.active::after {
  content: "";
  position: absolute;
  right: 20px;
  width: 8px;
  height: 8px;
  background: #fff;
  border-radius: 50%;
  box-shadow: 0 0 10px rgba(255, 255, 255, 0.8);
}

/* Welcome Message */
.welcome-message {
  text-align: center;
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  padding: 50px;
  border-radius: 24px;
  box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
  border: 1px solid var(--glass-border);
  max-width: 700px;
  margin: 0 auto;
  position: relative;
  overflow: hidden;
}

.welcome-message::before {
  content: "";
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(
    circle,
    rgba(99, 102, 241, 0.2) 0%,
    transparent 70%
  );
  animation: pulse 4s ease-in-out infinite;
  z-index: -1;
}

.welcome-message h2 {
  font-size: 38px;
  font-weight: 800;
  margin-bottom: 20px;
  background: linear-gradient(90deg, #fff, #a5b4fc);
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
  text-shadow: 0 5px 15px rgba(99, 102, 241, 0.3);
}

.welcome-message p {
  font-size: 18px;
  color: rgba(255, 255, 255, 0.8);
  max-width: 500px;
  margin: 0 auto;
  line-height: 1.6;
}

/* Cards Grid */
.subscription-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 25px;
  padding: 20px;
  max-width: 1400px;
  margin: 0 auto;
}

.subscription-card {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  border-radius: 20px;
  padding: 30px;
  text-align: center;
  cursor: pointer;
  transition: all 0.4s ease;
  position: relative;
  overflow: hidden;
  border: 1px solid var(--glass-border);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.subscription-card::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: var(--gradient-1);
  opacity: 0;
  transition: opacity 0.4s ease;
  z-index: -1;
}

.subscription-card:nth-child(2n)::before {
  background: var(--gradient-2);
}

.subscription-card:nth-child(3n)::before {
  background: var(--gradient-3);
}

.subscription-card:nth-child(4n)::before {
  background: var(--gradient-4);
}

.subscription-card:nth-child(5n)::before {
  background: var(--gradient-5);
}

.subscription-card:hover {
  transform: translateY(-10px);
  box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
  border-color: transparent;
}

.subscription-card:hover::before {
  opacity: 0.8;
}

.subscription-card:hover .icon-container {
  background: rgba(255, 255, 255, 0.2);
  transform: scale(1.1) rotate(5deg);
}

.icon-container {
  font-size: 40px;
  width: 80px;
  height: 80px;
  margin: 0 auto 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.15);
  transition: all 0.4s ease;
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
}

.card-title {
  font-size: 20px;
  font-weight: 700;
  margin-bottom: 12px;
  color: #fff;
  text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
}

.card-description {
  font-size: 16px;
  color: rgba(255, 255, 255, 0.8);
  line-height: 1.5;
}

/* Notification Container */
.notification-container {
  width: 100%;
  max-width: 1200px;
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  border-radius: 20px;
  box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
  overflow: hidden;
  border: 1px solid var(--glass-border);
  margin: 0 auto;
}

/* Tabs */
.tab-header {
  display: flex;
  border-bottom: 1px solid var(--glass-border);
  background: rgba(255, 255, 255, 0.05);
}

.tab-button {
  flex: 1;
  padding: 20px;
  background: none;
  border: none;
  font-size: 16px;
  font-weight: 600;
  color: rgba(255, 255, 255, 0.7);
  cursor: pointer;
  transition: all 0.3s ease;
  position: relative;
}

.tab-button:hover {
  color: #fff;
  background: rgba(255, 255, 255, 0.05);
}

.tab-button.active {
  color: #fff;
}

.tab-button.active::after {
  content: "";
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: var(--gradient-1);
  box-shadow: 0 0 15px rgba(99, 102, 241, 0.6);
}

.tab-content {
  min-height: 500px;
  padding: 30px;
  display: none;
  animation: fadeIn 0.5s ease;
}

.tab-content.active {
  display: block;
}

.tab-loading {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 200px;
  color: rgba(255, 255, 255, 0.7);
  font-size: 18px;
}

/* Buttons */
.btn {
  padding: 12px 24px;
  background: var(--gradient-1);
  color: #fff;
  border: none;
  border-radius: 12px;
  cursor: pointer;
  font-weight: 600;
  font-size: 16px;
  transition: all 0.3s ease;
  box-shadow: 0 8px 20px rgba(99, 102, 241, 0.4);
  position: relative;
  overflow: hidden;
  z-index: 1;
}

.btn::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(255, 255, 255, 0.2);
  transform: translateX(-100%);
  transition: transform 0.5s ease;
  z-index: -1;
}

.btn:hover {
  transform: translateY(-3px);
  box-shadow: 0 12px 25px rgba(99, 102, 241, 0.5);
}

.btn:hover::before {
  transform: translateX(0);
}

.btn:active {
  transform: translateY(1px);
}

/* Form Elements */
.form-row {
  display: flex;
  gap: 20px;
}

.form-group {
  flex: 1;
  display: flex;
  flex-direction: column;
}

label {
  font-size: 14px;
  font-weight: 600;
  color: #fff;
  margin-bottom: 8px;
}

input[type="text"],
select {
  width: 100%;
  box-sizing: border-box;
  padding: 12px;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 8px;
  color: #fff;
  font-size: 16px;
}

input[type="text"]::placeholder {
  color: rgba(255, 255, 255, 0.5);
}

select {
  appearance: none;
  background-image: url("data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciIHdpZHRoPSIyNCIgaGVpZ2h0PSIyNCIgdmlld0JveD0iMCAwIDI0IDI0Ij48cGF0aCBmaWxsPSIjZmZmIiBkPSJNMTIgMTVsLTUtNSAxMCAwLTE1IDV6Ii8+PC9zdmc+");
  background-position: right 12px center;
  background-repeat: no-repeat;
  background-size: 12px;
}

/* Logo Upload */
.logo-group {
  align-items: flex-start;
}

.logo-upload {
  border: 1px dashed rgba(255, 255, 255, 0.3);
  border-radius: 8px;
  padding: 20px;
  text-align: center;
  cursor: pointer;
  position: relative;
}

.upload-label {
  display: flex;
  flex-direction: column;
  align-items: center;
  color: rgba(255, 255, 255, 0.7);
  cursor: pointer;
}

.upload-icon {
  font-size: 24px;
  margin-bottom: 8px;
}

.logo-upload p {
  font-size: 12px;
  color: rgba(255, 255, 255, 0.5);
  margin-top: 8px;
}

#logo-preview {
  max-width: 100%;
  max-height: 100px;
  margin-top: 10px;
}

/* ====== PAGE SPECIFIC COMPONENTS ====== */
/* Company Details */
.company-container {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  padding: 30px;
  border-radius: 20px;
  box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
  border: 1px solid var(--glass-border);
  max-width: 1200px;
  margin: 0 auto;
}

.company-title {
  font-size: 28px;
  font-weight: 700;
  color: #fff;
  margin-bottom: 5px;
}

.company-subtitle {
  font-size: 16px;
  color: rgba(255, 255, 255, 0.7);
  margin-bottom: 30px;
}

.tab-button-company {
  flex: 0 auto;
  padding: 10px 20px;
  background: none;
  border: none;
  font-size: 16px;
  font-weight: 600;
  color: rgba(255, 255, 255, 0.7);
  cursor: pointer;
  transition: all 0.3s ease;
  position: relative;
  margin-right: 10px;
  border-radius: 8px 8px 0 0;
}

.tab-button-company:hover {
  color: #fff;
}

.tab-button-company.active {
  color: #fff;
  background: rgba(255, 255, 255, 0.1);
}

.tab-button-company .tab-icon {
  margin-right: 8px;
}

.tab-content-company {
  padding: 30px;
  display: none;
}

.tab-content-company.active {
  display: block;
}

.section-title {
  font-size: 20px;
  font-weight: 600;
  color: #fff;
  margin-bottom: 5px;
}

.section-subtitle {
  font-size: 14px;
  color: rgba(255, 255, 255, 0.7);
  margin-bottom: 30px;
}

form {
  display: grid;
  gap: 20px;
}

.save-btn {
  width: fit-content;
  align-self: flex-end;
  background: var(--primary);
  border-radius: 8px;
}

/* Payment Methods */
.icon-pill.type1 {
  background: rgba(0, 128, 255, 0.2);
}

.icon-pill.type2 {
  background: rgba(255, 128, 0, 0.2);
}

.modal-overlay {
  position: fixed;
  inset: 0;
  display: none;
  align-items: flex-start;
  justify-content: center;
  padding-top: 8vh;
  background: rgba(0, 0, 0, 0.55);
  z-index: 9999;
}

.modal {
  width: 680px;
  max-width: 95vw;
  background: rgba(255, 255, 255, 0.12);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);
  border: 1px solid var(--glass-border);
  border-radius: 16px;
  padding: 20px;
  box-shadow: 0 18px 60px rgba(0, 0, 0, 0.45);
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 14px;
}

.modal-title {
  font-size: 20px;
  font-weight: 700;
  color: #fff;
}

.modal-close {
  background: none;
  border: none;
  color: #fff;
  font-size: 22px;
  cursor: pointer;
}

.modal-row {
  display: flex;
  gap: 16px;
  margin-bottom: 14px;
}

.modal-row .form-group {
  flex: 1;
}

.modal-actions {
  display: flex;
  gap: 10px;
  justify-content: flex-end;
  margin-top: 4px;
}

.icon-grid {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
  margin-top: 8px;
}

.icon-pill {
  width: 64px;
  height: 42px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 10px;
  cursor: pointer;
  background-color: rgba(255, 255, 255, 0.14);
  border: 1px solid rgba(255, 255, 255, 0.2);
  transition: transform 0.15s ease, box-shadow 0.15s ease, background 0.2s ease;
  color: transparent;
}

.icon-pill:hover {
  transform: translateY(-1px);
}

.icon-pill.active {
  outline: 2px solid var(--primary);
  box-shadow: 0 0 0 4px rgba(99, 102, 241, 0.25);
  background-color: rgba(99, 102, 241, 0.18);
}

.payment-list {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 16px;
  margin-top: 16px;
}

.payment-card {
  background: rgba(255, 255, 255, 0.1);
  border: 1px solid var(--glass-border);
  border-radius: 16px;
  padding: 18px;
  box-shadow: 0 10px 28px rgba(0, 0, 0, 0.22);
}

.payment-card .title {
  font-weight: 700;
  color: #fff;
  margin-bottom: 6px;
}

.payment-card .meta {
  font-size: 13px;
  color: rgba(255, 255, 255, 0.75);
}

.payment-card .icon-pill {
  width: 48px;
  height: 36px;
}

/* Icon-specific backgrounds for colorful ATM card-like representations */
.icon-pill[data-icon="visa"] {
  background-image: url("data:image/svg+xml;base64,");
  background-size: contain;
  background-repeat: no-repeat;
  background-position: center;
}

.icon-pill[data-icon="mastercard"] {
  background-image: url("data:image/svg+xml;base64,");
  background-size: contain;
  background-repeat: no-repeat;
  background-position: center;
}

.icon-pill[data-icon="paypal"] {
  background-image: url("data:image/svg+xml;base64,");
  background-size: contain;
  background-repeat: no-repeat;
  background-position: center;
}

.icon-pill[data-icon="amex"] {
  background-image: url("data:image/svg+xml;base64,");
  background-size: contain;
  background-repeat: no-repeat;
  background-position: center;
}

.icon-pill[data-icon="applepay"] {
  background-image: url("data:image/svg+xml;base64,");
  background-size: contain;
  background-repeat: no-repeat;
  background-position: center;
}

.icon-pill[data-icon="googlepay"] {
  background-image: url("data:image/svg+xml;base64,");
  background-size: contain;
  background-repeat: no-repeat;
  background-position: center;
}

.icon-pill[data-icon="bank"] {
  background-image: url("data:image/svg+xml;base64,");
  background-size: contain;
  background-repeat: no-repeat;
  background-position: center;
}

.icon-pill[data-icon="cash"] {
  background-image: url("data:image/svg+xml;base64,PHN2ZyB2aWV3Qm94PSIwIDAgMjQgMjQiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PHBhdGggZD0iTTMgNmgxOHY1aDJWNEgxdjVoMlY2em0tMiA3aDIydjdoLTJ2LTVoLTE4djVIMXYtN3ptNCA5aDE0di01SDV2NXptNi0zaDJ2LTFoLTJ2MXoiIGZpbGw9IiM0Q0FGNTAiIC8+PC9zdmc+");
  background-size: contain;
  background-repeat: no-repeat;
  background-position: center;
}

.icon-pill[data-icon="other"] {
  background-image: url("data:image/svg+xml;base64,PHN2ZyB2aWV3Qm94PSIwIDAgMjQgMjQiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PHBhdGggZD0iTTQgNmgxNnYySDIR6m0wNGgxNnYySDIR6m04NGgxNnYySDIR6m04NGgxNnYySDIR6IiBmaWxsPSIjRkY5ODAwIiAvPjwvc3ZnPg==");
  background-size: contain;
  background-repeat: no-repeat;
  background-position: center;
}

/* Page Embed */
.page-embed {
  width: 100%;
  height: 600px;
  border: none;
  border-radius: 16px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
}

/* ====== ANIMATIONS ====== */
@keyframes float {
  0%,
  100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-10px);
  }
}

@keyframes pulse {
  0%,
  100% {
    transform: scale(1);
    opacity: 0.5;
  }
  50% {
    transform: scale(1.1);
    opacity: 0.8;
  }
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes float-blob {
  0%,
  100% {
    transform: translate(0, 0) scale(1);
  }
  33% {
    transform: translate(30px, -50px) scale(1.1);
  }
  66% {
    transform: translate(-20px, 20px) scale(0.9);
  }
}

/* ====== BACKGROUND ELEMENTS ====== */
.bg-blob {
  position: fixed;
  border-radius: 50%;
  filter: blur(80px);
  opacity: 0.4;
  z-index: -1;
  animation: float-blob 20s infinite ease-in-out;
}

.blob-1 {
  width: 400px;
  height: 400px;
  background: var(--primary);
  top: -100px;
  right: -100px;
}

.blob-2 {
  width: 300px;
  height: 300px;
  background: var(--accent);
  bottom: -50px;
  left: -50px;
  animation-delay: 2s;
}

.blob-3 {
  width: 350px;
  height: 350px;
  background: var(--secondary);
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  animation-delay: 5s;
}
/* ===== Compliance Stats ===== */
.stats-container {
  display: flex;
  gap: 20px;
  margin-bottom: 25px;
  justify-content: center;
}

.stat-box {
  flex: 1;
  min-width: 180px;
  padding: 20px;
  border-radius: 16px;
  color: #fff;
  text-align: center;
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.25);
  font-weight: 600;
}

.stat-box .stat-label {
  font-size: 14px;
  opacity: 0.85;
  margin-bottom: 6px;
}

.stat-box .stat-value {
  font-size: 28px;
  font-weight: 800;
}

.stat-box.purple {
  background: var(--gradient-1);
}
.stat-box.green {
  background: var(--gradient-4);
}
.stat-box.yellow {
  background: var(--gradient-5);
}
/* Full-width analytics card */
.subscription-card.full-span {
  grid-column: 1 / -1; /* span all columns */
  min-height: 180px;
  display: flex;
  align-items: center;
  gap: 24px;
  padding-left: 32px;
}

.subscription-card.full-span .icon-container {
  width: 64px;
  height: 64px;
  font-size: 36px;
}
/* Make form controls prefer dark UI on browsers that support it */
:root {
  color-scheme: dark;
}

/* ===== Dark dropdown fixes ===== */
select {
  color: #fff;
  background: rgba(255, 255, 255, 0.08);
  border: 1px solid rgba(255, 255, 255, 0.18);
  outline: none;
  caret-color: #fff;
}

select:focus {
  border-color: var(--primary);
  box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.35);
}

/* Popup list items (works in Chrome/Edge/Firefox) */
select option,
select optgroup {
  background-color: #0f172a; /* dark panel */
  color: #e5e7eb;
}

/* Selected/hovered option color */
select option:checked,
select option:hover {
  background-color: #1e293b !important;
  color: #fff !important;
}

/*REMOVING THE ANIMATION FOREVER*/
/* === GLOBAL: disable ALL animations & transitions everywhere === */
html *,
html *::before,
html *::after {
  animation: none !important;
  transition: none !important;
  scroll-behavior: auto !important;
}

/* Remove hover transforms/shadows that cause extra paint/composite */
.nav-link:hover,
.subscription-card:hover,
.payment-card:hover,
.btn:hover,
.icon-pill:hover {
  transform: none !important;
  box-shadow: none !important;
}

/* Disable glass/blur effects (GPU heavy) */
.sidebar,
.welcome-message,
.subscription-card,
.notification-container,
.company-container,
.modal {
  backdrop-filter: none !important;
  -webkit-backdrop-filter: none !important;
}

/* Kill decorative/animated backgrounds */
.bg-blob {
  display: none !important;
}
body::before {
  content: none !important;
}

/* Ensure any fade/slide-in targets render instantly */
.tab-content,
.company-container,
.welcome-message {
  opacity: 1 !important;
  transform: none !important;
}

/* === ULTRA-LEAN MODE (no animations, no gradients, no shadows, no blur) === */

/* already killed animations earlier, so now flatten visuals completely */
:root {
  /* optional: a single flat panel color */
  --panel: #0f172a; /* dark slate */
  --panel-soft: #0b1220; /* slightly darker for contrast bands */
}

/* 1) Remove box/text shadows everywhere */
*,
*::before,
*::after {
  box-shadow: none !important;
  text-shadow: none !important;
  filter: none !important;
}

/* 2) Kill gradients & pseudo gradient overlays */
[class*="gradient"],
.subscription-card::before,
.nav-link::before,
.tab-button.active::after,
.stat-box.purple,
.stat-box.green,
.stat-box.yellow,
.btn {
  background-image: none !important;
}

/* Replace common gradient backgrounds with a flat panel */
.subscription-card,
.notification-container,
.company-container,
.sidebar,
.welcome-message,
.modal,
.btn,
.nav-link.active,
.tab-header,
.tab-button.active,
.stat-box {
  background: var(--panel) !important;
}

/* Slight banding where it helps hierarchy */
.tab-header {
  background: var(--panel-soft) !important;
}
.nav-link.active {
  background: var(--panel-soft) !important;
}

/* 3) Remove transparency to avoid compositing work */
.subscription-card,
.notification-container,
.company-container,
.sidebar,
.welcome-message,
.modal,
.payment-card,
.icon-container,
input[type="text"],
select {
  background-color: var(--panel) !important;
  border-color: rgba(255, 255, 255, 0.12) !important; /* keep a faint divider */
}

/* 4) Square corners (rounded corners can increase antialias work on some GPUs) */
.subscription-card,
.payment-card,
.company-container,
.notification-container,
.modal,
.btn,
.icon-container,
.logo-icon,
.tab-button-company,
.tab-content-company,
.page-embed,
.stat-box,
.nav-link,
input[type="text"],
select {
  border-radius: 12px !important;
}

/* 5) Keep hover calm (no lifts / glows) */
.nav-link:hover,
.subscription-card:hover,
.payment-card:hover,
.btn:hover {
  transform: none !important;
  box-shadow: none !important;
  border-color: rgba(255, 255, 255, 0.12) !important;
  background: var(--panel-soft) !important; /* tiny visual cue only */
}

/* 6) Ensure instant visibility for views that used fade/slide */
.tab-content,
.company-container,
.welcome-message {
  opacity: 1 !important;
  transform: none !important;
}

/* 7) Extra: flatten logo treatment */
.logo span {
  text-shadow: none !important;
}
.logo-icon {
  box-shadow: none !important;
}

/* 8) Background ornaments already disabled earlier; keep them off */
.bg-blob {
  display: none !important;
}
body::before {
  content: none !important;
}
.subscription-card,
.payment-card {
  border: 2px solid var(--secondary) !important; /* #8b5cf6 */
  border-radius: 6px !important; /* or 8px if you prefer */
}
/* Purple border around the outer dashboard panel */
.company-container {
  border: 2px solid var(--secondary) !important; /* #8b5cf6 */
  border-radius: 8px !important; /* match your tile rounding */
}

/* optional: make the tab header divider match the theme */
.company-container .tab-header {
  border-bottom: 1px solid rgba(139, 92, 246, 0.35) !important;
}

/* optional: apply the same border to other large panels */
.welcome-message,
.notification-container,
.modal {
  border: 2px solid var(--secondary) !important;
  border-radius: 8px !important;
}
/* KPIs with the same border */
.stat-box {
  border: 2px solid var(--secondary) !important;
  border-radius: 6px !important;
}

/*LIGHT THEME IMPLEMENTATION*/
/* =========================
   THEME TOKENS + MAPPINGS
   ========================= */

/* Dark (current look) */
:root[data-theme="dark"] {
  /* App background */
  --app-bg: linear-gradient(135deg, #1a1c3d 0%, #2d1b69 50%, #0f0c29 100%);

  /* Surfaces */
  --panel: #0f172a;
  --panel-soft: #0b1220;

  /* Text colors */
  --text: #e5e7eb;
  --muted: #94a3b8;

  /* Dividers / borders */
  --border: rgba(148, 163, 184, 0.18);

  /* Brand */
  --primary: #6366f1;
  --secondary: #8b5cf6;
  --accent: #ec4899;

  /* (kept for any places that still use them) */
  --gradient-1: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  --gradient-2: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
}

/* Light — Ocean Breeze */
:root[data-theme="light"] {
  /* App background */
  --app-bg: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 50%, #f8fafc 100%);

  /* Surfaces */
  --panel: #ffffff;
  --panel-soft: #f0f9ff;

  /* Text colors */
  --text: #0f172a;
  --muted: #475569;

  /* Dividers / borders (slight cyan tint) */
  --border: rgba(2, 132, 199, 0.25);

  /* Brand */
  --primary: #0ea5e9; /* Sky Blue */
  --secondary: #06b6d4; /* Cyan (used by your borders) */
  --accent: #0891b2;

  /* Ocean gradients (if you ever re-enable them somewhere) */
  --gradient-1: linear-gradient(135deg, #0ea5e9 0%, #0284c7 100%);
  --gradient-2: linear-gradient(135deg, #06b6d4 0%, #0891b2 100%);
}

/* Apply theme tokens to the app (no animations involved) */
body {
  background: var(--app-bg) !important;
  color: var(--text) !important;
}

/* Muted text */
.company-subtitle,
.card-description,
label,
.payment-card .meta {
  color: var(--muted) !important;
}

/* Generic surface/border sync (works with your ultra-lean overrides) */
.sidebar,
.welcome-message,
.subscription-card,
.notification-container,
.company-container,
.modal,
.payment-card,
.stat-box,
.nav-link,
input[type="text"],
select {
  background: var(--panel) !important;
  border-color: var(--border) !important;
  color: var(--text) !important;
}

/* Soft surface for active header bands */
.nav-link.active,
.tab-header {
  background: var(--panel-soft) !important;
}

/* Your “neon border” uses --secondary (purple in dark, cyan in light) */
.subscription-card,
.payment-card,
.company-container,
.welcome-message,
.notification-container,
.modal {
  border: 2px solid var(--secondary) !important;
}

/* If you rounded corners earlier, they’ll carry across themes.
   If you want a specific radius here, uncomment:
   border-radius: 8px !important;
*/
/* Fix: make the theme toggle full width and non-clipped */
.sidebar-actions {
  padding: 12px 16px 16px;
}

.theme-toggle {
  display: inline-flex; /* flex row for icon + text */
  align-items: center;
  gap: 10px;
  width: 100%; /* fill sidebar width */
  box-sizing: border-box;
  padding: 10px 12px;
  background: var(--panel) !important;
  color: var(--text) !important;
  border: 1px solid var(--border) !important;
  border-radius: 6px !important;
  white-space: nowrap; /* keep “☀️ Light” / “🌙 Dark” on one line */
  overflow: visible; /* avoid clipping */
}

.theme-toggle .toggle-icon {
  width: 20px; /* reserve space for the emoji */
  flex: 0 0 20px;
  text-align: center;
}

.theme-toggle .toggle-text {
  flex: 1 1 auto; /* let text expand */
}
/* ================================
   THEME TEXT TOKENS + GLOBAL MAP
   (append-only, no other edits needed)
   ================================ */

/* Dark defaults (used today) */
:root {
  --text-1: #e5e7eb; /* primary text on dark */
  --text-2: #cbd5e1; /* secondary text on dark */
  --text-muted: #94a3b8; /* muted/subtle on dark */
}

/* Light theme overrides (Ocean Breeze) */
:root[data-theme="light"] {
  --text-1: #1e293b; /* Primary Text */
  --text-2: #64748b; /* Secondary Text */
  --text-muted: #94a3b8; /* Muted Text */
}

/* If you use a page background variable, keep this too */
:root {
  --page-bg: linear-gradient(135deg, #1a1c3d 0%, #2d1b69 50%, #0f0c29 100%);
}
:root[data-theme="light"] {
  --page-bg: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 50%, #f8fafc 100%);
}
body {
  background: var(--page-bg);
}

/* -------- Global typography mapping --------
   These !important bumps safely override your earlier #fff / rgba() colors
   without you hunting them all down. Remove !important later if you refactor.
*/
body,
.company-title,
.section-title,
.card-title,
.payment-card .title,
.modal-title {
  color: var(--text-1) !important;
}

.company-subtitle,
.section-subtitle,
.card-description,
.payment-card .meta,
label,
.nav-link,
.tab-button,
.tab-button-company,
.tab-loading,
input,
select,
button.btn {
  color: var(--text-2) !important;
}

.logo-upload p,
input::placeholder,
.muted,
.muted-text {
  color: var(--text-muted) !important;
}

/* Keep active/contrast items readable in both themes */
.nav-link.active,
.btn {
  color: #fff !important; /* buttons/active nav remain high-contrast */
}
/* =======================
   LIGHT THEME TWEAKS
   ======================= */

/* 1) Stronger active color in the LEFT SIDEBAR (light mode) */
:root[data-theme="light"] .nav-link {
  color: #64748b;
}

:root[data-theme="light"] .nav-link:hover {
  background: rgba(14, 165, 233, 0.08);
  color: #0c4a6e;
}

:root[data-theme="light"] .nav-link.active {
  background: rgba(14, 165, 233, 0.12) !important; /* subtle fill */
  border: 1px solid #0ea5e9 !important;           /* requested color */
  color: #0c4a6e !important;
  box-shadow: 0 0 0 2px rgba(14, 165, 233, 0.08) inset;
}

:root[data-theme="light"] .nav-link.active::after {
  background: #0ea5e9 !important;                 /* the little dot on the right */
  box-shadow: 0 0 10px rgba(14, 165, 233, 0.6);
}

/* Optional: borders for large content cards/frames to match the accent */
:root[data-theme="light"] .company-container,
:root[data-theme="light"] .notification-container {
  border-color: #0ea5e9 !important;
}

/* 2) Make ALL text inside MAIN CONTENT black in light mode */
:root[data-theme="light"] .main-content {
  color: #000 !important;
}

/* Force black for common text elements within main content */
:root[data-theme="light"] .main-content h1,
:root[data-theme="light"] .main-content h2,
:root[data-theme="light"] .main-content h3,
:root[data-theme="light"] .main-content h4,
:root[data-theme="light"] .main-content h5,
:root[data-theme="light"] .main-content h6,
:root[data-theme="light"] .main-content p,
:root[data-theme="light"] .main-content label,
:root[data-theme="light"] .main-content .card-title,
:root[data-theme="light"] .main-content .card-description,
:root[data-theme="light"] .main-content .section-title,
:root[data-theme="light"] .main-content .section-subtitle,
:root[data-theme="light"] .main-content .stat-label,
:root[data-theme="light"] .main-content .stat-value,
:root[data-theme="light"] .main-content .tab-button,
:root[data-theme="light"] .main-content .tab-button-company,
:root[data-theme="light"] .main-content .payment-card .title,
:root[data-theme="light"] .main-content .payment-card .meta {
  color: #000 !important;
}

/* Keep buttons readable in both themes */
:root[data-theme="light"] .main-content .btn {
  color: #fff !important;
}
/* =========================
   Light theme: fix button contrast
   ========================= */
:root[data-theme="light"] .main-content .btn {
  /* Stronger, brand gradient */
  background: linear-gradient(135deg, #0ea5e9 0%, #0284c7 100%) !important;
  border: 1px solid #0284c7 !important;
  color: #fff !important;            /* keep text readable */
  box-shadow: 0 6px 16px rgba(2, 132, 199, 0.25);
}

/* Remove the white overlay that makes it look pale */
:root[data-theme="light"] .main-content .btn::before {
  display: none !important;
}

/* Optional: clearer hover without washing out the color */
:root[data-theme="light"] .main-content .btn:hover {
  filter: brightness(0.95);
}

/* Links in main content should be visible too */
:root[data-theme="light"] .main-content a {
  color: #0ea5e9;
}
/* =========================
   Sidebar & Brand (Light)
   ========================= */
:root[data-theme="light"] .sidebar {
  background: #ffffff;
  border-right: 1px solid #e2e8f0;
  backdrop-filter: blur(8px);
}

.logo {
  padding: 0 24px 24px;
  border-bottom: 1px solid #e2e8f0;
}

/* Brand text next to logo */
.logo .brand-left,
.logo .brand-right {
  font-size: 24px;
  font-weight: 800;
  line-height: 1;
}

:root[data-theme="light"] .logo .brand-left {         /* “Subs” */
  color: #1e293b; /* dark slate */
}

:root[data-theme="light"] .logo .brand-right {        /* “Tracker” */
  background: linear-gradient(135deg, #0ea5e9 0%, #0284c7 100%);
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
}

/* Keep the square logo icon looking crisp in light */
:root[data-theme="light"] .logo-icon {
  box-shadow: 0 10px 22px rgba(2, 132, 199, 0.18);
}

/* =========================
   Sidebar nav look
   ========================= */
:root[data-theme="light"] .nav-menu {
  padding: 12px 15px;
}

:root[data-theme="light"] .nav-link {
  color: #64748b;                  /* medium slate */
  border-radius: 16px;
  padding: 14px 18px;
  font-weight: 700;
  background: transparent;
  box-shadow: none;
}

/* Remove the dark-mode hover wash effect in light */
:root[data-theme="light"] .nav-link::before { display: none; }

:root[data-theme="light"] .nav-link:hover {
  background: #f1f5f9;             /* subtle hover */
  color: #1e293b;
}

/* ACTIVE = pill with Ocean Breeze gradient + white text + glow + white dot */
:root[data-theme="light"] .nav-link.active {
  background: linear-gradient(135deg, #0ea5e9 0%, #0284c7 100%);
  color: #ffffff;
  box-shadow:
    0 10px 22px rgba(2, 132, 199, 0.25),
    inset 0 1px 0 rgba(255, 255, 255, 0.20);
  position: relative;
}

/* The white dot at the right side of the active pill */
:root[data-theme="light"] .nav-link.active::after {
  content: "";
  position: absolute;
  right: 16px;
  top: 50%;
  transform: translateY(-50%);
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: #ffffff;
  box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.35),
              0 0 14px rgba(2, 132, 199, 0.55);
}

/* Make sure icons or dots inside the active pill stay visible */
:root[data-theme="light"] .nav-link.active * { color: #ffffff; }

/* =========================
   Main content text (for safety)
   ========================= */
:root[data-theme="light"] .main-content,
:root[data-theme="light"] .main-content * {
  color: #1e293b; /* force readable text in content area */
}

/* Buttons inside main content already fixed earlier; keep the good contrast */
:root[data-theme="light"] .main-content .btn {
  background: linear-gradient(135deg, #0ea5e9 0%, #0284c7 100%) !important;
  border: 1px solid #0284c7 !important;
  color: #fff !important;
  box-shadow: 0 6px 16px rgba(2, 132, 199, 0.25);
}
:root[data-theme="light"] .main-content .btn::before { display: none !important; }
:root[data-theme="light"] .main-content .btn:hover { filter: brightness(0.95); }
